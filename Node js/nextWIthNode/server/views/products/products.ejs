<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <p>Products List</p>
    <ul>
        <% if(products.length !== 0){ %>
            <% for(let p of products){ %>
                <li>
                    <%= p.name %>
                    <a href="/products/edit/<%= p.id %>">Edit</a>
                    <a href="/products/del/<%= p.id %>"">Delete</a>
                </li>
            <% } %>
        <% } %>
            <a href="/products/add">Add product</button>
    </ul>
</body>
</html>